package frc.robot.subsystems.Intake;

import edu.wpi.first.math.geometry.Rotation2d;
import edu.wpi.first.math.util.Units;

public class IntakeConstants {
    //TODO: find the real numbers
    public static final Rotation2d PIVOT_MAX_ANGLE = new Rotation2d(Units.degreesToRadians(70));
    public static final double WIDTH_METERS = 0.85;
    public static final double MAX_EXTENSION_METERS = 0.3;
}